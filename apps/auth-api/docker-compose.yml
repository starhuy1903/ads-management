version: '3.8'
services:
  db:
    image: postgres:alpine
    ports:
      - 5432:5432
    restart: always
    env_file:
      - .env
  cache:
    image: redis:alpine
    restart: always
    ports:
      - '6379:6379'
    env_file:
      - .env
